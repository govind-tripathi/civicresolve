# Contributing

1. Create a branch for your change.
2. Keep changes focused.
3. Run `python -m py_compile app.py`.
4. Run `python -m unittest discover -s tests -v`.
5. Update the README when behavior or setup changes.
